{
  "name": "serveur_api",
  "version": "1.0.0",
  "description": "Serveur HTTP pour api rest",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    
    "sequelize:database1:migrate": "sequelize --options-path ./.sequelize-Real_data --env Real_data db:migrate",
    "sequelize:database1:migrate:undo": "sequelize --options-path ./.sequelize-Real_data --env Real_data db:migrate:undo",
    "sequelize:database1:seed:all": "sequelize --options-path ./.sequelize-Real_data --env Real_data db:seed:all",

    "sequelize:database2:migrate": "sequelize --options-path ./.sequelize-Simulate_data --env Simulate_data db:migrate",
    "sequelize:database2:migrate:undo": "sequelize --options-path ./.sequelize-Simulate_data --env Simulate_data db:migrate:undo",
    "sequelize:database2:seed:all": "sequelize --options-path ./.sequelize-Simulate_data --env Simulate_data db:seed:all"

  },
  "keywords": [
    "server",
    "http",
    "api",
    "rest"
  ],
  "author": "Dethoor Clement",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.6",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.3.5"
  }
}
